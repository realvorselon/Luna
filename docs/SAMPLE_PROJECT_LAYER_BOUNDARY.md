# Sample Project Layer Boundary

This is a concise public-safe boundary note for making the sample project layer feel less meta and more natural while preserving the Luna loop. This run does not change `prototype.html`, add code, add behavior, add controls, or add persistence.

The prototype currently proves the Luna loop, but the sample project layer still feels a little framework-like or meta in the current state area. The next useful improvement is to make the fictional recipe-note sample project feel more like a small project someone could actually return to.

The framework should remain underneath: one project state, Current Mode, current goal, what changed last, one Next Action, Ignore For Now, Record the Change, and Rest.

A later polish may adjust sample project copy so the recipe-note project feels more natural. It may make the current state area sound less like a product explanation and more like the state of the fictional project. It may keep labels clear while making card body text more project-specific. It may reduce meta wording where the page is talking about the Luna loop instead of the sample project.

Any later polish should be copy-only unless absolutely necessary to preserve existing behavior.

Do not remove the Luna loop. Do not change the prototype structure. Do not add new behavior. Do not add more persistence. Do not add more editable cards. Do not add more buttons or controls. Do not add Save, Reset, Export, Import, Sync, Account, Login, Share, Delete, or Clear buttons.

Do not add sessionStorage, cookies, backend, accounts, database, authentication, sync, analytics, app architecture, pricing, deployment planning, LLM implementation, dashboard expansion, visual redesign, mode workflow, multi-project system, or new Philosophy Garden content.

Preserve project name and current goal local browser saving. Preserve the clear-local-edits control behavior.

Preserve Current Mode, last completed run, Next Action, Ignore For Now, Record the Change, Rest / next step, and every other card as static and unsaved.

Preserve the dashboard ↔ prototype reversible path and “Return to dashboard” link.

Next, rest before deciding whether to implement one tiny copy-only polish that makes the sample project layer feel less meta and more natural, and before adding any new behavior, more persistence, more editable cards, sessionStorage, cookies, backend, accounts, database, sync, dashboard expansion, visual redesign, mode workflow, multi-project system, or new Philosophy Garden content.
